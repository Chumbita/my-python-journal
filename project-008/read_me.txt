Proyecto 8:
Se crea una función que toma una tupla como entrada y devuelve un diccionario que contiene la frecuencia de cada elemento en la tupla. 

Project 8:
A function is created that takes a tuple as input and returns a dictionary containing the frequency of each element in the tuple.