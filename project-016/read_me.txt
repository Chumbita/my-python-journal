Proyecto 16: 
Se crearon dos funciones, una que recibe un número decimal y lo convierte en binario y la otra que recibe un número binario y devuelve su respectivo valor en decimal. 

Project 16:
Two functions were created, one that receives a decimal number and converts it to binary and the other that receives a binary number and returns its respective value in decimal.