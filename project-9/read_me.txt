Proyecto 9: 
Se crea una función que recibe como entrada una lista, elimina elementos duplicados y devuelve un set que contiene los elementos únicos.

Project 9:
A function is created that receives a list as input, removes duplicate items, and returns a set containing the unique items.