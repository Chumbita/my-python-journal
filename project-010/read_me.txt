Proyecto 10:
Se creó una función que recibe dos sets como entrada, los une en un nuevo set y luego encuentra e imprime la intersección y la diferencia entre los dos sets orginales.

Project 10:
Created a function that receives two sets as input, joins them into a new set, and then finds and prints the intersection and difference between the two original sets.